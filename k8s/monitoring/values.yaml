# Grafana configuration
grafana:
  adminUser: admin
  adminPassword: cTcTgLE62nItyZzp
  service:
    type: NodePort 

# Prometheus server configuration
prometheus:
  server:
    service:
      type: NodePort 

# Optional: disable unnecessary components
alertmanager:
  enabled: false
pushgateway:
  enabled: false
kubeStateMetrics:
  enabled: true
nodeExporter:
  enabled: true
